name: project
description: "Aplikasi pendamping mahasiswa Universitas Terbuka untuk manajemen mata kuliah dan jadwal belajar."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.2.1
  flutter_markdown_plus: ^1.0.1
  infinite_scroll_pagination: ^5.1.1
  supabase_flutter: ^2.6.0
  google_fonts: ^8.0.1
  flutter_inappwebview: ^6.1.5
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true